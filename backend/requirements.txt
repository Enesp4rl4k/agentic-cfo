# Web framework
fastapi==0.141.1
uvicorn[standard]==0.52.4

# Payments
stripe==10.5.0

# LangGraph + LangChain
# The versions the test suite passes on. This file used to pin langgraph 0.2.0
# with langchain-core 0.2.10, which pip cannot install together (langgraph 0.2.0
# needs langchain-core>=0.2.27) — no environment was ever built from it, and the
# one the tests ran in had langgraph 0.1.19. CI installs from this file on
# Python 3.12, the version the suite runs on.
langgraph==0.1.19
langchain==0.2.17
langchain-openai==0.1.25
langchain-core==0.2.43

# Database
sqlalchemy==2.0.52
alembic==1.19.1
asyncpg==0.29.0
psycopg2-binary==2.9.9
aiosqlite==0.20.0
pgvector==0.3.5

# Redis / job queue
redis==5.0.7
arq==0.28.0

# PDF / OCR
pymupdf==1.28.2
pytesseract==0.3.13
Pillow==12.3.0
pdfplumber==0.11.3    # Table-aware PDF extraction (optional but recommended)

# Excel
openpyxl==3.1.5

# PDF generation + templating
weasyprint==69.0
Jinja2>=3.1.4    # HTML template rendering for PDF reports

# Validation
pydantic==2.13.4
pydantic-settings==2.15.0

# HTTP client
httpx==0.28.1

# Scheduling
apscheduler==3.10.4

# Scientific / stats (anomaly detection + ML)
scipy==1.13.1
numpy==1.26.4
# ML anomaly detection (Sprint L1) — graceful fallback if not installed
scikit-learn>=1.4.0
# Time-series forecasting (Sprint L1) — STL + ARIMA
statsmodels>=0.14.0

# Email notifications
aiosmtplib==5.1.2

# Utilities
python-multipart==0.0.32
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
aiofiles==25.1.0

# Testing
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==5.0.0
pytest-randomly==3.15.0    # randomised test order — surfaces cross-test state pollution
playwright==1.47.0         # scripts/page_sweep.py — renders every page against a live app
lxml==6.1.2                # tests/test_edefter_xbrl.py — XSD validation against GİB's own
                           # edefter.xsd. The generator itself is stdlib ElementTree; only
                           # the check needs a schema validator.
pyyaml==6.0.3              # tests/test_ci_database.py — reads .github/workflows/ci.yml so
                           # the workflow is held to what it claims. Pinned rather than
                           # inherited: a guard that skips when its parser is absent
                           # passes exactly where it is needed.

# Linting / type checking
ruff==0.16.4    # pinned — ruff.toml selects the rule set; CI gate is blocking
mypy==2.3.1

sentry-sdk>=1.40.0
cryptography>=42.0.0

# Imported by the app and missing from this file until the first CI install:
# bcrypt (password hashing — a clean install could not log anyone in),
# email-validator (pydantic EmailStr: auth and every router importing it was
# skipped), reportlab (board deck / SMMM PDFs), tiktoken (context budgeting).
bcrypt==5.0.0
email-validator==2.3.0
reportlab==5.0.1
tiktoken==0.14.0
