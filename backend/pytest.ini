[pytest]
asyncio_mode = auto
pythonpath = .
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    eval: golden-case evaluation of end-to-end agent output; CI gates on pass-rate
    connectors: Connector Platform (Faz 13) — adapter + runner + kernel-flip contract
